#!/bin/sh

set -e

case $1 in
    syn | kjv)
        python3 formatter.py "txt/$1.txt" | less -M
        ;;
    *)
        printf "Usage: %s <syn | kjv>\n" "$(basename "$0")"
        exit 1
        ;;
esac
